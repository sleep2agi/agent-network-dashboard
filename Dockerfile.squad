FROM node:20-alpine
WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci --no-audit --no-fund

COPY . .
ARG NEXT_PUBLIC_DISABLE_SSE=1
ENV NEXT_PUBLIC_DISABLE_SSE=$NEXT_PUBLIC_DISABLE_SSE
RUN npm run build

ENV NODE_ENV=production
ENV PORT=3000
EXPOSE 3000
CMD ["npm", "start"]
